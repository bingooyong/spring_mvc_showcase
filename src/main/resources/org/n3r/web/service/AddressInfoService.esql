[findAddressInfo]
SELECT
  id,
  name,
  email,
  phone
FROM mvc_address_info
WHERE id = ##;

[findAllAddressInfo]
SELECT
  id,
  name,
  email,
  phone
FROM mvc_address_info;


[onSaveAddressInfo]
INSERT INTO MVC_ADDRESS_INFO (ID, NAME, EMAIL, PHONE) VALUES (#id#, #name#, #email#, #phone#);